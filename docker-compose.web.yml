services:
  mcp_massive:
    build: .
    container_name: mcp_massive_web
    ports:
      - "${MCP_PORT:-8000}:8000"
    environment:
      - MASSIVE_API_KEY=${MASSIVE_API_KEY}
      - MCP_TRANSPORT=streamable-http
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
    restart: unless-stopped
